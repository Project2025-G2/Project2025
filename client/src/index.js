import React from 'react';
import ReactDOM from 'react-dom/client'; // Зверни увагу на /client
import App from './App.jsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
