# Вибираємо базовий образ Node.js для React
FROM node:18

# Встановлюємо робочу директорію для клієнтської частини
WORKDIR /app

# Копіюємо package.json та package-lock.json для встановлення залежностей
COPY package*.json ./

# Встановлюємо всі залежності
RUN npm install

# Копіюємо весь код клієнта в контейнер
COPY . .

# Створюємо оптимізовану версію для продакшн
RUN npm run build

# Вказуємо порт, на якому клієнт буде працювати
EXPOSE 3000

# Команда для запуску клієнтської частини
CMD ["npm", "start"]
